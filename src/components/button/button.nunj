{% macro button(props) %}
<button class="button {% if props.className %}{{ props.className}}{% endif %}" type="{% if props.type %}{{ props.type }}{% else %}button{% endif %}">
	{{ props.children }}
</button>
{% endmacro %}
